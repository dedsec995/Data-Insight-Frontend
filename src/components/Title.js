// src/components/Header.js
import React from 'react';
import { Typography } from '@mui/material';

const Title = () => {
    return (
        <Typography variant="h6" gutterBottom>
            Please Upload the file to continue.
        </Typography>
    );
};

export default Title;
